# CLAUDE.md

Project instructions are defined in `AGENTS.md`.

Before making any change:
1. Read and follow `AGENTS.md`.
2. Read the relevant files in `docs/`.
3. Treat `docs/01_PROJECT_CONTEXT.md` through `docs/06_DEMO_FLOW.md` as the project requirements.
4. Use `docs/07_COURSE_SCOPE_AND_REFERENCES.md` for course alignment, complexity limits, and modernization guidance.

Do not install Laravel Boost or any additional package unless the user explicitly approves it.

Do not run migrations, seeders, setup scripts, or commands that modify the database unless the current task explicitly requires them.
